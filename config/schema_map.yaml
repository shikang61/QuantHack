# Vendor CSV column -> canonical column (see src/mt5_trader/data/schema.py).
# PLACEHOLDER: the real schema is released after the opening announcement.
# Edit this file when the dump arrives, then run scripts/ingest_dump.py.
columns:
  time: ts
  sym: symbol
  # bid / ask already match canonical names. L2 list cols (bidprices/bidsizes/
  # askprices/asksizes) are dropped by normalise() (kept set = ALL_COLUMNS).
