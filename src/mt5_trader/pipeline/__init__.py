"""Strategy development pipeline: discovery diagnostics → backtest/validate →
deploy, plus the strategy stash (a holding pen for sub-par candidates worth
iterating on). Driven by scripts/strategy.py. See docs/WORKFLOW.md."""
